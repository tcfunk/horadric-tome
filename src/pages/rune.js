import * as React from 'react'
import { graphql } from "gatsby";

import Layout from '../components/layout';

class Page extends React.Component {
  render() {
    return (
      <Layout>
        @todo: for selected rune, show runewords it's part of
      </Layout>
    )
  }
}

export default Page

